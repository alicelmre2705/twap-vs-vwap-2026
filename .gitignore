# Python
__pycache__/
*.pyc
.venv/
venv/
.env
.pytest_cache/

# OS
.DS_Store
__MACOSX/

# Data (downloaded/generated — re-created by run_all.sh)
data/raw/
data/clean/

# Full experiment results (large, regenerated by pipeline)
results/tables/experiment_results.csv

# Keep sample CSV, summary tables, and figures for README rendering
# After running the pipeline, commit updated figures with:
#   git add results/figures/ results/tables/ && git commit
